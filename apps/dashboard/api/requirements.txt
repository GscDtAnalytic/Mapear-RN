# Runtime deps do backend FastAPI do dashboard.
# sentence-transformers + anthropic alimentam /narratives/search (busca RAG):
# o embedding da query usa o MESMO modelo do corpus (mpnet, 768d) e a resposta
# é gerada pela API da Anthropic. torch é instalado CPU-only no Dockerfile.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
google-cloud-bigquery>=3.17,<4.0
pandas>=2.0,<3.0
cachetools>=5.3,<6.0
db-dtypes>=1.2,<2.0
sentence-transformers>=3.1,<4.0
anthropic>=0.39
